# Task Tracker

- [ ] Set `time_tolerance_ticks` to 500 in all top-level JSON configs under `json/`
- [ ] Re-run full sample set with updated 500-tick tolerance and capture outputs
- [ ] Extend `analyze_clusters` to compute matching quality metrics and regenerate the PDF report
